<table class="use">
    <tr>
        {% for item in NAVIGATION %}
        <td>
            <a href="?SHOW_SETTLERLIST=1&mark={{ item.mark }}&order={{ ORDER_BY }}&way={{ SORT_ORDER }}"
                class="{{ item.cssclass }}">{{ item.page }}</a>
        </td>
        {% endfor %}
    </tr>
</table>
