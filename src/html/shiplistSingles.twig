{# needs following variables: SINGLESHIPWRAPPERS) #}
{% from 'html/shiplistmacros.twig' import shiplist_header, shiplist_body %}

<table id="singleshiplist" class="tcal" id="shiplist_singles_table">
    <tr>
        <td colspan="11">Einzelschiffe</td>
    </tr>
    {{ shiplist_header() }}
    {% for wrapper in SINGLESHIPWRAPPERS %}
    {{ shiplist_body(wrapper) }}
    {% endfor %}
</table>
