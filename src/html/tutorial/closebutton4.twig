<button id="tutorial-close-button" class="cardassian-close-button">
    <div class="close-button-frame">
        <div class="button-inner">
            <span class="close-symbol">&#x2715;</span>
        </div>
    </div>
</button>

<style>
.cardassian-close-button {
    position: absolute;
    width: 80px;
    height: 80px;
    background-color: transparent;
    border: none;
    padding: 0;
    cursor: pointer;
    z-index: 1002;
    clip-path: polygon(
        50% 0%,
        100% 25%,
        100% 75%,
        50% 100%,
        0% 75%,
        0% 25%
    );
    transition: all 0.3s ease;
    overflow: hidden;
}

.close-button-frame {
    position: relative;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #7b664b, #604829);
    clip-path: inherit;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    overflow: hidden;
}

.button-inner {
    position: absolute;
    width: 90%;
    height: 90%;
    background-color: #d9c4a9;
    clip-path: inherit;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: inset 0 0 5px rgba(255, 255, 255, 0.3);
    transition: transform 0.3s ease, background-color 0.3s ease;
}

.close-symbol {
    color: #604829;
    font-size: 28px;
    font-weight: bold;
    text-shadow: 0 0 8px rgba(96, 72, 41, 0.6);
    transition: all 0.2s ease;
}

.cardassian-close-button:hover .button-inner {
    background-color: #f0e6d2;
    animation: scanAnimation 1s infinite;
}

.cardassian-close-button:active .button-inner {
    transform: scale(0.95);
    box-shadow: inset 0 0 5px rgba(255, 255, 255, 0.3);
}

.cardassian-close-button:hover .close-symbol {
    color: #7b664b;
    text-shadow: 0 0 12px rgba(123, 102, 75, 0.8);
}

@keyframes scanAnimation {
    0% {
        box-shadow: inset 0 0 10px rgba(255, 255, 255, 0.5);
    }
    50% {
        box-shadow: inset 0 0 20px rgba(255, 255, 255, 0.8);
    }
    100% {
        box-shadow: inset 0 0 10px rgba(255, 255, 255, 0.5);
    }
}
</style>
