<button id="tutorial-close-button" class="romulan-close-button">
    <div class="button-frame">
        <div class="button-inner">
            <span class="close-symbol">&#x2715;</span>
        </div>
    </div>
</button>

<style>
.romulan-close-button {
    position: absolute;
    width: 65px;
    height: 65px;
    background-color: #232220;
    border: none;
    padding: 0;
    cursor: pointer;
    z-index: 1002;
    clip-path: polygon(
        0 0,
        100% 0,
        85% 100%,
        0 85%
    );
    transition: all 0.3s ease;
    overflow: hidden;
}

.button-frame {
    position: relative;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #40c48b, #32906d);
    clip-path: polygon(
        2px 2px,
        calc(100% - 2px) 2px,
        calc(85% - 2px) calc(100% - 2px),
        2px calc(85% - 2px)
    );
}

.button-inner {
    position: absolute;
    top: 2px;
    left: 2px;
    right: 2px;
    bottom: 2px;
    background-color: #1b1b1b;
    display: flex;
    align-items: center;
    justify-content: center;
    clip-path: polygon(
        1px 1px,
        calc(100% - 1px) 1px,
        calc(85% - 1px) calc(100% - 1px),
        1px calc(85% - 1px)
    );
}

.close-symbol {
    color: #5a99ac;
    font-size: 28px;
    font-weight: bold;
    text-shadow: 0 0 8px rgba(90, 153, 172, 0.6);
    transition: all 0.2s ease;
}

.romulan-close-button:hover {
    transform: scale(1.08);
}

.romulan-close-button:hover .close-symbol {
    color: #84e3c5;
    text-shadow: 0 0 12px rgba(132, 227, 197, 0.8);
}

.romulan-close-button:hover::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent 45%, rgba(255,255,255,0.1) 50%, transparent 55%);
    animation: scanline 2s linear forwards;
}

.romulan-close-button:active {
    transform: scale(0.95);
}

.romulan-close-button:active .button-frame {
    background: linear-gradient(135deg, #32906d, #267292);
}

@keyframes scanline {
    0% {
        transform: translateY(-100%);
    }
    100% {
        transform: translateY(200%);
    }
}
</style>
