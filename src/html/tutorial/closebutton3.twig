<button id="tutorial-close-button" class="klingon-close-button">
    <div class="button-inner">
        <span class="close-symbol">&#x2715;</span>
    </div>
</button>

<style>
.klingon-close-button {
    position: absolute;
    width: 70px;
    height: 70px;
    background-color: #363134;
    border: none;
    padding: 0;
    cursor: pointer;
    z-index: 1002;
    clip-path: polygon(
        20px 0,
        100% 0,
        100% calc(100% - 20px),
        calc(100% - 20px) 100%,
        0 100%,
        0 20px
    );
    transition: transform 0.2s ease-out;
}

.button-inner {
    position: relative;
    width: calc(100% - 6px);
    height: calc(100% - 6px);
    margin: 3px;
    background-color: #511a1f;
    clip-path: polygon(
        18px 0,
        100% 0,
        100% calc(100% - 18px),
        calc(100% - 18px) 100%,
        0 100%,
        0 18px
    );
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 
        inset 0 1px 0 rgba(255,255,255,0.2),
        0 2px 4px rgba(0,0,0,0.3);
}

.close-symbol {
    color: #c9a63e;
    font-size: 32px;
    font-weight: bold;
    transform: rotate(0deg);  
    text-shadow: 0 0 10px rgba(201, 166, 62, 0.6);
}

.klingon-close-button:hover {
    transform: scale(1.05);
}

.klingon-close-button:hover .close-symbol {
    color: #f59e42;
    text-shadow: 0 0 15px rgba(245, 158, 66, 0.8);
}

.klingon-close-button:active {
    transform: scale(0.95);
}

.klingon-close-button:active .button-inner {
    box-shadow: 
        inset 0 2px 4px rgba(0,0,0,0.4),
        0 1px 2px rgba(0,0,0,0.2);
}
</style>
