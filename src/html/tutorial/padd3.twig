<div id="padd-popup" class="padd-popup">
    <div id="padd-screen" class="padd-container">
        <div class="padd-upper">
            <div class="text-field-container">
                <div class="text-field">
                    <div class="triangle-pattern"></div>
                    <div class="text-content">
                        <div id="padd-title"></div>
                        <div id="padd-text"></div>
                    </div>
                </div>
            </div>
        </div>
        <div class="padd-lower">
            <div class="nav-buttons">
                <div id="back-button" class="nav-button">
                    <span class="nav-arrow">◀</span>
                </div>
                <div id="next-button" class="nav-button">
                    <span class="nav-arrow">▶</span>
                </div>
            </div>
            <div class="decorative-buttons">
                <div class="button-column red">
                    <div class="deco-button"></div>
                    <div class="deco-button"></div>
                    <div class="deco-button"></div>
                    <div class="deco-button"></div>
                </div>
                <div class="connector"></div>
                <div class="button-column blue">
                    <div class="deco-button"></div>
                    <div class="deco-button"></div>
                    <div class="deco-button"></div>
                    <div class="deco-button"></div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
:root {
    --klingon-gray: #464145;
    --klingon-border: #511a1f;
    --klingon-pattern: #854857;
    --clip-length: 40px;
    --text-margin: 30px;
    --containerwidth: 420px;
    --triangle-size: 45px;
}

.padd-popup {
    position: fixed;
    top: 10%;
    left: 100%;
    transform: translate(-50%, 0%);
    z-index: 1003;
    cursor: move;
    transition: left 0.5s ease-out;
    width: var(--containerwidth);
    display: flex;
    justify-content: center;
}

.padd-container {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    transform: perspective(1000px) rotateX(2deg);
}

.padd-upper {
    position: relative;
    width: 300px;
    height: 350px;
    background-color: #363134;
    clip-path: polygon(
        var(--clip-length) 0,
        calc(100% - var(--clip-length)) 0,
        100% var(--clip-length),
        100% 100%,
        var(--clip-length) 100%,
        0 calc(100% - var(--clip-length)),
        0 var(--clip-length)
    );
    box-shadow: 0 4px 8px rgba(0,0,0,0.3);
}

.padd-upper::before {
    content: '';
    position: absolute;
    top: 4px;
    left: 4px;
    right: 4px;
    bottom: 0;
    background-color: var(--klingon-gray);
    clip-path: polygon(
        var(--clip-length) 0,
        calc(100% - var(--clip-length)) 0,
        100% var(--clip-length),
        100% 100%,
        var(--clip-length) 100%,
        0 calc(100% - var(--clip-length)),
        0 var(--clip-length)
    );
}


.padd-lower {
    position: relative;
    width: 260px;
    height: 275px;
    background-color: #363134;
    margin-top: -1px;
    margin-left: var(--clip-length);
    clip-path: polygon(
        0 0,
        100% 0,
        100% calc(100% - var(--clip-length)),
        calc(100% - var(--clip-length)) 100%,
        var(--clip-length) 100%,
        0 calc(100% - var(--clip-length))
    );
    box-shadow: 0 4px 8px rgba(0,0,0,0.3);
}

.padd-lower::before {
    content: '';
    position: absolute;
    top: 0;
    left: 4px;
    right: 4px;
    bottom: 4px;
    background-color: var(--klingon-gray);
    clip-path: polygon(
        0 0,
        100% 0,
        100% calc(100% - var(--clip-length)),
        calc(100% - var(--clip-length)) 100%,
        var(--clip-length) 100%,
        0 calc(100% - var(--clip-length))
    );
}


.text-field-container {
    position: absolute;
    top: 15px;
    left: 15px;
    width: calc(100% - 30px);
    height: calc(100% - 30px);
    background-color: var(--klingon-border);
    clip-path: polygon(
        var(--clip-length) 0,
        calc(100% - var(--clip-length)) 0,
        100% var(--clip-length),
        100% calc(100% - var(--clip-length)),
        calc(100% - var(--clip-length)) 100%,
        var(--clip-length) 100%,
        0 calc(100% - var(--clip-length)),
        0 var(--clip-length)
    );
    box-shadow: 
        inset 0 2px 4px rgba(0,0,0,0.5),
        inset 0 -1px 2px rgba(255,255,255,0.1);
    border: 1px solid rgba(0,0,0,0.3);
}



.text-field {
    position: absolute;
    top: 5px;
    left: 5px;
    width: calc(100% - 10px);
    height: calc(100% - 10px);
    background-color: #000000;
    clip-path: polygon(
        var(--clip-length) 0,
        calc(100% - var(--clip-length)) 0,
        100% var(--clip-length),
        100% calc(100% - var(--clip-length)),
        calc(100% - var(--clip-length)) 100%,
        var(--clip-length) 100%,
        0 calc(100% - var(--clip-length)),
        0 var(--clip-length)
    );
    overflow: hidden;
}

.triangle-pattern {
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    z-index: 1;
    background-image:
        linear-gradient(0deg, 
            transparent 0%, transparent 49%, 
            rgba(133, 72, 87, 0.3) 49%, rgba(133, 72, 87, 0.3) 51%, 
            transparent 10%, transparent 50%
        ),
        linear-gradient(135deg, 
            rgba(182, 74, 100, 0.05) 0%, 
            rgba(182, 74, 100, 0.05) 49%, 
            rgba(133, 72, 87, 0.3) 49%, rgba(133, 72, 87, 0.3) 51%, 
            transparent 51%, transparent 50%
        ),
        linear-gradient(225deg, 
            transparent 0%, transparent 49%, 
            rgba(133, 72, 87, 0.3) 49%, rgba(133, 72, 87, 0.3) 51%, 
            rgba(182, 74, 100, 0.05) 51%, rgba(182, 74, 100, 0.05) 50%
        );
    background-size: var(--triangle-size) var(--triangle-size);
}


.text-content {
    position: relative;
    z-index: 2;
    padding: 20px;
    height: 100%;
    display: flex;
    flex-direction: column;
}

#padd-title {
    color: #822c02;
    text-align: center;
    margin-bottom: 15px;
    font-size: 22px;
    font-weight: 600;
}

#padd-text {
    color: #f59e42;
    text-align: left;
    overflow-y: auto;
    scrollbar-width: thin;
    scrollbar-color: #c08438 #000000;
    font-weight: 600;
    font-size: 17px;
}

#padd-text::-webkit-scrollbar {
    width: 8px;
}

#padd-text::-webkit-scrollbar-track {
    background: #000000;
}

#padd-text::-webkit-scrollbar-thumb {
    background: #c08438;
    border-radius: 4px;
}



.nav-buttons {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    display: flex;
    justify-content: space-between;
    padding: 0 10px;
}

.nav-button {
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    clip-path: polygon(20px 0, calc(100% - 20px) 0, 100% 50%, calc(100% - 20px) 100%, 20px 100%, 0 50%);
    background-color: #773b3d;
    cursor: pointer;
    position: relative;
    border: 1px solid rgba(0,0,0,0.2);
    box-shadow: 
        0 2px 0 rgba(0,0,0,0.3),
        inset 0 1px 0 rgba(255,255,255,0.2);
    transition: all 0.1s ease-out;
}



#back-button {
    width: 90px;
}

#next-button {
    width: 130px;
}

.nav-button:hover {
    box-shadow: 
        0 3px 0 rgba(0,0,0,0.3),
        inset 0 1px 0 rgba(255,255,255,0.2);
}

.nav-button:active {
    box-shadow: 
        0 0 0 rgba(0,0,0,0.3),
        inset 0 1px 2px rgba(0,0,0,0.2);
    transform: translateY(2px);
}
.nav-button.inactive {
    background-color: #5f2c2e;
    cursor: not-allowed;
    transform: none;
    box-shadow: none;
}

.nav-button.inner-update {
    background-color: #5f2c2e;
    cursor: not-allowed;
    transform: none;
    box-shadow: none;
}

.nav-arrow {
    color: #c9a63e;
        color: #c9a63e;
    font-size: 24px;
    user-select: none;
}

.nav-button.inactive .nav-arrow,
.nav-button.inner-update .nav-arrow {
    color: #8b7429;
}

.decorative-buttons {
    position: absolute;
    top: 60px;
    left: 10px;
    right: 10px;
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 10px;
}

.button-column {
    display: flex;
    flex-direction: column;
    width: 100px;
}

.deco-button {
    cursor: pointer;
    box-shadow: 
        0 2px 0 rgba(0,0,0,0.3),
        inset 0 1px 0 rgba(255,255,255,0.2);
    transition: all 0.1s ease-out;
}

.deco-button:hover {
    box-shadow: 
        0 3px 0 rgba(0,0,0,0.3),
        inset 0 1px 0 rgba(255,255,255,0.2);
}

.deco-button:active {
    box-shadow: 
        0 0 0 rgba(0,0,0,0.3),
        inset 0 1px 2px rgba(0,0,0,0.2);
    transform: translateY(2px);
}

.button-column.red .deco-button:nth-child(1) {
    height: 55px;
    margin-left:15px;
    background-color: #511a1f;
    clip-path: polygon(0 0, 100% 0, 100% 100%, 0 50%);
}

.button-column.red .deco-button:nth-child(2) {
    height: 60px;
    margin-left:15px;
    background-color: #511a1f;
    clip-path: polygon(0 0%, 100% 27px, 100% 100%, 0 100%); 
    margin-top: -25px;
}

.button-column.red .deco-button:nth-child(3) {
    position: relative;  
    height: 100px;
    margin-left: 15px;
    background-color: #511a1f;
    clip-path: polygon(0 0, 100% 0, 100% 13.5%, 0 73%);
    margin-top: 3px;
}

.button-column.red .deco-button:nth-child(3)::after {
    content: '';
    position: absolute;
    top: 7px;
    left: 5%;
    width: 90%;
    height: 6px;
    background-color: #131313;
    border-radius: 3px;
    z-index: 1;
}

.button-column.red .deco-button:nth-child(4) {
    height: 90px;
    margin-left:15px;
    background-color: #511a1f;
    clip-path: polygon(0px 60px, 100% 0, 100% 100%, 31px 100%);
    margin-top: -83px;
}

.button-column.blue .deco-button:nth-child(1) {
    position: relative; 
    height: 100px;
    margin-top: 10px;
    margin-right: 10px;
    background-color: #343530;
    clip-path: polygon(0 0, 100% 0%, 100% 45%, 0 100%);
}

.button-column.blue .deco-button:nth-child(1)::after {
    content: '';
    position: absolute;
    top: 7px;
    left: 5%;
    width: 90%;
    height: 6px;
    background-color: #131313;
    border-radius: 3px;
    z-index: 1;
}

.button-column.blue .deco-button:nth-child(2) {
    height: 90px;
    margin-right: 10px;
    background-color: #343530;
    clip-path: polygon(0 60%, 100% 0%, 100% 40%, 0 75%);
    margin-top: -51px;
}

.button-column.blue .deco-button:nth-child(3) {
    height: 75px;
    margin-right: 10px;
    background-color: #343530;
    clip-path: polygon(0 45%, 100% 5%, 100% 100%, 0 75%); 
    margin-top: -53px;
}

.button-column.blue .deco-button:nth-child(4) {
    height: 50px;
    margin-right: 10px;
    background-color: #343530;
    clip-path: polygon(0 2%, 100% 20px, 70% 50px, 15px 75%); 
    margin-top: -16px;
}

.connector {
    width: 20px;
    height: 185px;
    background-color: #511e1a;
    margin-top: -5px;
}



</style>
