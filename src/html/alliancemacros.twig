{% macro alliance_avatar(alliance, style) %}
{% if alliance.getAvatar %}
<img src="{{ ASSET_PATHS.alliance }}/{{ alliance.getAvatar }}.png" {% if style %}style="{{ style }}" {% endif %} />
{% endif %}
{% endmacro %}