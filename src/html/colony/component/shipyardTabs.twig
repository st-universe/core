<table class="tcal" style="width: 40vw;">
    {% set fieldId = FIELD.getId %}
    <tr>
        {% if IS_FIGHTER_SHIPYARD %}
        <td class="colonymenu {{ getColonyMenuClass(CURRENT_MENU, 9) }}">
            <a class="colmenubutton" href="javascript:void(0);"
                onclick="switchColonySubmenu(10, null, {{ FUNC.getId }}, {{ fieldId }});">
                <span>Schiffbau</span>
            </a>
        </td>
        {% else %}
        <td class="colonymenu {{ getColonyMenuClass(CURRENT_MENU, 9) }}">
            <a class="colmenubutton" href="javascript:void(0);"
                onclick="switchColonySubmenu(8, null, {{ FUNC.getId }}, {{ fieldId }});">
                <span>Schiffbau</span>
            </a>
        </td>
        {% endif %}
        <td class="colonymenu {{ getColonyMenuClass(CURRENT_MENU, 18) }}">
            <a class="colmenubutton" href="javascript:void(0);"
                onclick="switchColonySubmenu(18, null, {{ FUNC.getId }}, {{ fieldId }});">
                <span>Schiffsreparatur</span>
            </a>
        </td>
        <td class="colonymenu {{ getColonyMenuClass(CURRENT_MENU, 17) }}">
            <a class="colmenubutton" href="javascript:void(0);"
                onclick="switchColonySubmenu(17, null, {{ FUNC.getId }}, {{ fieldId }});">
                <span>Schiffsdemontage</span>
            </a>
        </td>
        <td class="colonymenu {{ getColonyMenuClass(CURRENT_MENU, 19) }}">
            <a class="colmenubutton" href="javascript:void(0);"
                onclick="switchColonySubmenu(19, null, {{ FUNC.getId }}, {{ fieldId }});">
                <span>Schiffsumrüstung</span>
            </a>
        </td>
    </tr>
</table>